name: Hello-docker-server-prod
on: 
  workflow_dispatch:
jobs:
  deploy-hello-job-prod:
    permissions:
         packages: write
         contents: read
    uses: ./.github/workflows/Common.yml
    with:
      env_name: prod

